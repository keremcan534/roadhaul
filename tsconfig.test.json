{
  // Unit and architecture tests (Vitest, Node): the pure layers, plus presentation
  // code that runs without WebGL (three.js scene graphs work headless).
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.test.tsbuildinfo",
    "lib": ["ES2022"],
    "types": ["vite/client"]
  },
  "include": ["tests/unit", "tests/architecture", "tests/support"]
}
